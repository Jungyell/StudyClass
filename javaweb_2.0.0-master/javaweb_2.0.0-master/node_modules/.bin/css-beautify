// CSS 파일을 정리하고 예쁘게 만드는 도구
#!/bin/sh
//basedir 설정
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")  // 현재 스크립트 파일의 디렉터리 경로를 설정, 경로 구분자를 \에서 /로 변경

//windows 호환성 처리
case `uname` in  // 현재 운영체제 확인
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac  // 운영 체제가 CYGWIN, MINGW, MSYS 중 하나인 경우, cygpath -w 명령을 사용하여 Unix 스타일 경로를 Windows 스타일 경로로 변환
// node 실행 경로 확인 및 실행
if [ -x "$basedir/node" ]; then  // basedir 경로에 node 실행 파일이 있는지 확인
  exec "$basedir/node"  "$basedir/../js-beautify/js/bin/css-beautify.js" "$@"  //  basedir 경로의 node 실행 파일을 사용하여 css-beautify.js 스크립트 실행, "$@"는 모든 전달된 인수를 포함
else 
  exec node  "$basedir/../js-beautify/js/bin/css-beautify.js" "$@"  // // basedir 경로에 node 실행 파일이 없으면, 시스템에 설치된 node 명령을 사용하여 css-beautify.js 스크립트 실행
fi
