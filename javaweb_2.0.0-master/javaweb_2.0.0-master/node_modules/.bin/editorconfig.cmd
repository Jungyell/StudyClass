@ECHO off  // 명령 프롬프트에서 명령을 실행할 때 명령어를 표시하지 않도록 설정
GOTO start  // 'start' 레이블로 이동

:find_dp0  // 'find_dp0' 레이블 정의
SET dp0=%~dp0  // 스크립트의 드라이브와 경로를 dp0 변수에 저장
EXIT /b  // 현재 배치 스크립트의 실행을 종료

:start  // 'start' 레이블 정의
SETLOCAL  // 환경 변수의 변경 범위를 현재 배치 파일로 제한
CALL :find_dp0  // 'find_dp0' 레이블 호출

IF EXIST "%dp0%\node.exe" (  // dp0 경로에 node.exe가 존재하는지 확인
  SET "_prog=%dp0%\node.exe"  // 존재하면 _prog 변수에 해당 경로의 node.exe 설정
) ELSE (  // 존재하지 않으면
  SET "_prog=node"  // _prog 변수에 시스템 경로의 node 설정
  SET PATHEXT=%PATHEXT:;.JS;=;%  // PATHEXT 환경 변수에서 .JS 제거
)

endLocal & goto #_undefined_# 2>NUL || title %COMSPEC% & "%_prog%"  "%dp0%\..\editorconfig\bin\editorconfig" %*  // 환경 변수 설정 범위를 끝내고, _prog 변수를 사용하여 editorconfig 스크립트를 실행
