@ECHO off  // 명령어 출력을 표시하지 않도록 설정
GOTO start  // start 레이블로 이동
:find_dp0
SET dp0=%~dp0  // 현재 스크립트 파일의 디렉터리 경로를 dp0 변수에 설정
EXIT /b  //  find_dp0 레이블의 실행을 종료하고 이전에 호출된 위치로 돌아감
:start
SETLOCAL  // 로컬 환경 변수 설정
CALL :find_dp0  // find_dp0 레이블을 호출하여 dp0 변수 설정

IF EXIST "%dp0%\node.exe" (  // dp0 경로에 node.exe 파일이 있는지 확인
  SET "_prog=%dp0%\node.exe"  // 있으면 _prog 변수에 해당 경로 설정
) ELSE (
  SET "_prog=node"  // 없으면 _prog 변수를 node로 설정
  SET PATHEXT=%PATHEXT:;.JS;=;%  // PATHEXT 환경 변수에서 .JS 확장자 제거
)

endLocal & goto #_undefined_# 2>NUL || title %COMSPEC% & "%_prog%"  "%dp0%\..\js-beautify\js\bin\css-beautify.js" %*  // 로컬 환경 설정을 종료하고, _prog 변수를 사용하여 css-beautify.js 스크립트 실행 %*는 모든 전달된 인수 포함
