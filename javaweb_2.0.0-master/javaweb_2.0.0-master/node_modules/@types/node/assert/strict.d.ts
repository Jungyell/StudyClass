// 'assert/strict' 모듈의 타입 정의를 선언
declare module 'assert/strict' {
    // 'node:assert' 모듈에서 'strict' 네임스페이스를 가져와서 내보냄
    import { strict } from 'node:assert';
    export = strict;
}

// 'node:assert/strict' 모듈의 타입 정의를 선언
declare module 'node:assert/strict' {
    // 'node:assert' 모듈에서 'strict' 네임스페이스를 가져와서 내보냄
    import { strict } from 'node:assert';
    export = strict;
}
